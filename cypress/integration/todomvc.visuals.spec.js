///<reference types="cypress" />

/*
import { TodoPage } from "../page-objects/todoPage"
import * as todoPageFns from "../page-objects/todoPage"

const todoPage = new TodoPage()
describe('check visual looks good', () => {
    before(() => todoPage.navigate())
    beforeEach(() => cy.eyesOpen({appName:'POC Cypress todoMVC', batchName:'todoMVC batch'}))
    afterEach(() => cy.eyesClose())

    it('add to do list and mark completed', () => {
        cy.eyesCheckWindow('primary screen')
        todoPage.addTodoList("Clean room")
        todoPage.addTodoList("clean bedroom")
        cy.eyesCheckWindow('todos addeded')

        todoPageFns.toggleNthTodoList(0)
        cy.eyesCheckWindow('completed first todo')
    })
})
*/